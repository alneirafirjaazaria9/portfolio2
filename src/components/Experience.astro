---
import Section from "./Section.astro";
import type { ExperienceProps } from "@types";

interface Props {
  experience: ExperienceProps[];
}

const { experience } = Astro.props;
---

<Section text="Work Experience" href="experience">
  {
    experience.map(({ company, position, location, startDate, endDate, summary, image }) => (
      <div class="mb-14 flex flex-col md:flex-row gap-12 items-start">
        <div class="flex-1">
          {/* Posisi (Judul Utama) */}
          <h3 class="mb-1.5 font-serif text-3xl font-bold text-white">
            {position}
          </h3>
          
          {/* Organisasi */}
          <h4 class="mb-4 text-xl font-semibold text-primary">
            {company}
          </h4>
          
          <div class="flex flex-wrap gap-3 mb-6">
            <div class="flex items-center gap-2 px-3 py-1.5 rounded-lg border border-white/10 bg-white/5 text-xs text-white/70 font-medium">
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/></svg>
              {location}
            </div>
            <div class="flex items-center gap-2 px-3 py-1.5 rounded-lg border border-white/10 bg-white/5 text-xs text-white/70 font-medium">
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg>
              {startDate} — {endDate}
            </div>
          </div>
          
          {/* List Poin (Kembali ke style checklist check.svg) */}
          {Array.isArray(summary) ? (
            <ul class="list-none">
              {summary.map((log) => (
                <li class="relative mb-3 pl-8 text-base text-neutral text-justify before:absolute before:top-1 before:left-0 before:content-[url(/check.svg)]">
                  {log}
                </li>
              ))}
            </ul>
          ) : (
            <p class="text-base text-neutral text-justify">{summary}</p>
          )}
        </div>

        {image && (
          <div class="w-full md:w-80 flex-shrink-0">
            <div class="flex flex-col gap-4">
               <div class="relative overflow-hidden rounded-2xl border border-white/10 shadow-2xl">
                 <img src={Array.isArray(image) ? image[0] : image} alt={company} class="object-cover w-full h-auto transition-transform duration-500" />
               </div>
            </div>
          </div>
        )}
      </div>
    ))
  }
</Section>